# Copy to .env and fill in. .env is gitignored - never commit real keys.
# OPENAI_API_KEY is not required for this submission - by instructor
# approval, the OpenAI slots are reached through Groq instead. Leave it
# blank or omit it.
OPENROUTER_API_KEY=sk-or-...
GROQ_API_KEY=gsk_...
